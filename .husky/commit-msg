npx commitlint --edit $1
echo "Avaliando variáveis sensíveis..."

FILES=$(git diff --cached --name-only --diff-filter=ACM)

detect-secrets-hook \
    --baseline .secrets.baseline \
    $FILES

if [ $? -ne 0 ]; then
    echo "❌ Possíveis segredos encontrados."
    exit 1
fi